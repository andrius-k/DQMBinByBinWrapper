# DQM bin by bin comparison wrapper

A tool that downloads all PR and baseline root files and initiates a bin by bin comparison.

## Dependencies:
- CMSSW with `compareDQMOutput.py` tool must be available

